export interface DividerProps {
  width?: string;  // Width (e.g., "w-full", "w-3/4")
  height?: string; // Thickness (e.g., "h-1", "h-2", "h-4")
  color?: string;  // Tailwind Background color (e.g., "bg-gray-300")
  margin?: string; // Spacing above/below (e.g., "my-4")
  paddingX?: string; // Side padding (e.g., "px-4", "px-8")
  className?: string; // Extra Tailwind classes
}
