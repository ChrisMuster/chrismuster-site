export interface PdfViewerProps {
  fileUrl: string; // The URL or path of the PDF file
  linkText?: string; // Optional link text
  border?: boolean; // Optional border toggle
  shadow?: boolean; // Optional shadow toggle
}
